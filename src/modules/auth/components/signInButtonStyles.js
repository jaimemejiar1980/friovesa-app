import { StyleSheet } from "react-native";

export const signInInButtonStyles = StyleSheet.create({
    container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    button: {
        width: 280,
        height: 44,
    },
});